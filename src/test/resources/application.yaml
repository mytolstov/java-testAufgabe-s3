spring:
  application:
    name: java-testaufgabe-s3-newDepartment-test

  datasource:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

aws:
  s3:
    bucket: test-bucket
    region: eu-central-1
    prefix: exports